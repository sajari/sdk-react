import SEO from '../components/SEO';

<SEO title="Search UI" description="Easy to use search components to quickly build a beautiful search interface." />

# Array Match

```jsx
function Example() {
  const pipeline = new Pipeline(
    {
      endpoint: 'https://jsonapi-au-staging-valkyrie.sajari.net',
      account: '1609911031581245493',
      collection: 'products',
    },
    'app',
  );

  const fields = new FieldDictionary({
    title: 'Product_Name',
    subtitle: 'Material',
    description: 'Imprintarea',
    image: 'Product_List_View_Image',
    price: 'Setup_Fee',
  });

  const tierStartQuantityFilter = new RangeFilterBuilder({
    name: 'tier-start-quantity',
    field: 'TierStartQuantity',
    group: 'group-1',
    min: 0,
    max: 2000,
    aggregate: false,
  });

  const tierPriceFilter = new RangeFilterBuilder({
    name: 'tier-price',
    field: 'TierPrice',
    group: 'group-1',
    min: 0,
    max: 10,
    aggregate: false,
  });

  const colorFilter = new FilterBuilder({
    name: 'color',
    field: 'Product_Color',
    group: 'group-1',
  });

  const App = React.memo(() => {
    const { searched } = useSearchContext();

    return (
      <>
        <Input />

        <div className="flex items-center justify-end mt-3">
          <Summary size="sm" />

          <div className="flex space-x-4 ml-auto">
            <Sorting
              options={[
                { name: 'Most relevant', value: '' },
                { name: 'Name: A to Z', value: 'Product_Name' },
                { name: 'Name: Z to A', value: '-Product_Name' },
                { name: 'Min Price: Low to High', value: 'TierPriceMin' },
                { name: 'Min Price: High to Low', value: '-TierPriceMin' },
                { name: 'Max Price: Low to High', value: 'TierPriceMax' },
                { name: 'Max Price: High to Low', value: '-TierPriceMax' },
                { name: 'Minimum Quantity: Low to High', value: 'Minimum_Quantity' },
                { name: 'Minimum Quantity: High to Low', value: '-Minimum_Quantity' },
              ]}
              size="sm"
            />
            <ResultsPerPage size="sm" />
            <ViewType size="sm" />
          </div>
        </div>

        <div className="flex mt-6">
          {searched && (
            <div className="w-1/4 pr-4 mr-4 border-gray-100 border-r space-y-6">
              <Filter type="range" name="tier-start-quantity" title="Budget" tick={500} step={50} />
              <Filter type="range" name="tier-price" title="Price" format="price" tick={2} />
              <Filter type="color" name="color" title="Color" />
            </div>
          )}

          <div className="flex-1">
            <Results defaultAppearance="list" />
          </div>
        </div>

        <div className="sticky bottom-0 p-6">
          <Pagination />
        </div>
      </>
    );
  });

  return (
    <SearchProvider
      search={{
        pipeline,
        fields,
        filters: [tierStartQuantityFilter, tierPriceFilter, colorFilter],
      }}
      searchOnLoad
    >
      <App />
    </SearchProvider>
  );
}
```
