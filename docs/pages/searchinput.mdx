import SEO from '../components/SEO';

<SEO
  title="SearchInput"
  description="SearchInput component is a component that is used to get user input in a text field. It is usually used together with a FormControl to provide an accessible label, validation messages, etc."
/>

# SearchInput

SearchInput component is a component that is used to get user input in a text field. It is usually used together with a FormControl to provide an accessible label, validation messages, etc.

## Import

```js
import { SearchInput } from 'sajari/react-search-ui';
```

## Usage

```jsx
<SearchInput label="Search something" onChange={(e) => {}} />
```

### Loading

```jsx
<SearchInput loading />
```

## Speech recognition

```jsx
<SearchInput enableVoice onVoiceInput={(text) => console.info(text)} />
```

## Props

`SearchInput` component props inherits the `input` element props with some additional for speech recognition and loading state

| Name           | Type                  | Default | Description                                                                                                                      |
| -------------- | --------------------- | ------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `loading`      | `boolean`             | `false` | Show the spinner icon                                                                                                            |
| `enableVoice`  | `boolean`             | `false` | Whether to enable speech recognition ([Compatibility table](https://developer.mozilla.org/en-US/docs/Web/API/SpeechRecognition)) |
| `onVoiceInput` | `(r: string) => void` |         | The callback when a voice transcript is output                                                                                   |
