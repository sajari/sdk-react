import SEO from '../../components/SEO';

<SEO title="SearchProvider" description="The SearchProvider component..." />

# SearchProvider

The `SearchProvider` should be used as a wrapper for the entire application to allow the search UI components to function.

```js
import { SearchProvider } from '@sajari/react-search-ui';
```

## Props

TBD
