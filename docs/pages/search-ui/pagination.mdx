import SEO from '../../components/SEO';

<SEO title="Pagination" description="The Pagination component renders a pagination component with config wired in." />

# Pagination

The Pagination component renders a pagination component with config wired in.

```js
import { Pagination } from '@sajari/react-search-ui';
import { SearchContextProvider, useSearch } from '@sajari/react-hooks';
```

## Usage

```jsx
function Example() {
  const pipeline = new Pipeline(
    {
      account: '1594153711901724220',
      collection: 'bestbuy',
      endpoint: 'https://jsonapi-us-valkyrie.sajari.net',
    },
    'query',
  );

  const variables = new Variables({ resultsPerPage: 3 });

  const SearchPlayground = React.memo(() => {
    useSearch('iphone');

    return (
      <>
        <Results appearance="grid" />
        <div style={{ display: 'flex', justifyContent: 'center', marginTop: '30px' }}>
          <Pagination />
        </div>
      </>
    );
  });

  return (
    <SearchContextProvider search={{ pipeline, variables, fields: { title: 'name', subtitle: 'brand' } }}>
      <SearchPlayground />
    </SearchContextProvider>
  );
}
```

## Props

| Name | Type | Default | Description |
| ---- | ---- | ------- | ----------- |
