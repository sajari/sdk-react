import SEO from '../../components/SEO';

<SEO
  title="Summary"
  description="Summary is a component used to display display the number of results and the latency from a search query."
/>

# Summary

Summary is a component used to display display the number of results and the latency from a search query.

```js
import { Summary } from '@sajari/react-search-ui';
```

## Usage

```jsx
function Example() {
  useSearch({ queryOverride: 'iphone' });

  return <Summary />;
}
```

### Override a query via autocomplete

```jsx
function Example() {
  return (
    <div className="flex flex-col space-y-3">
      <Input mode="suggestions" />
      <Summary suggest />
    </div>
  );
}
```
