---
name: RangeInput
menu: Components
route: /components/range-input
---

import { Playground } from "docz";
import { ExampleContainer } from "../../docs/helpers.ts";
import ThemeProvider from "../../styles/theming";
import { RangeFilter } from "../../controllers";
import RangeInput from "./index";
import { EVENT_SELECTION_UPDATED } from "../../events";

# RangeInput

## Usage

<Playground>
  {() => {
    const rangeFilter = new RangeFilter('range', [0, 500]);
    rangeFilter.set(200,300);
    const [filterOutput, setFilterOutput ] = React.useState(rangeFilter.filter());

    rangeFilter.listen(EVENT_SELECTION_UPDATED, () => {
      setFilterOutput(rangeFilter.filter())
    });

    return (
      <ExampleContainer>
        <ThemeProvider importantStyles={false}>
          <RangeInput 
            filter={rangeFilter} />
            <pre>"{filterOutput}"</pre>
        </ThemeProvider>
      </ExampleContainer>
    )
  }}
</Playground>

The `RangeInput` inherits all props from `RangeSlider` component with additional props.

### Custom input

<Playground>
  {() => {
    const rangeFilter = new RangeFilter('range', [0, 500]);
    rangeFilter.set(200,300);
    const [filterOutput, setFilterOutput ] = React.useState(rangeFilter.filter());

    rangeFilter.listen(EVENT_SELECTION_UPDATED, () => {
      setFilterOutput(rangeFilter.filter())
    });

    return (
      <ExampleContainer>
        <ThemeProvider importantStyles={false}>
          <RangeInput 
            filter={rangeFilter} 
            leftInput={({getCustomInputProps}) => {
              const {labelProps, inputProps} = getCustomInputProps()
              return (
                <div style={{display: "flex"}}>
                  <label {...labelProps}>Label</label>
                  <input {...inputProps} />
                </div>
              )
            }}
            rightInput={({getCustomInputProps}) => {
              const {labelProps, inputProps} = getCustomInputProps()
              return (
                <div style={{display: "flex"}}>
                  <label {...labelProps}>Label</label>
                  <input {...inputProps} />
                </div>
              )
            }}
          />
          <pre>"{filterOutput}"</pre>
        </ThemeProvider>
      </ExampleContainer>
    )
  }}
</Playground>
